sorts State;

signature
  f : Int Int -> State// ,
  // loop : Int Int -> State,
  // done : Int -> State,
  // extra : State State -> State
  ;

variables
  B : Bool, S : Int, N : Int, I : Int;

smt-unify
  (f N N)
  (f N N);
